# Requirements :

1. Ubuntu/Debian // NOTE :  
2. gcc version >= 14.2.0

## Setup Commands

1. sudo apt update
2. sudo apt install build-essential libgtk-3-dev libx11-dev pkg-config
3. mkdir ./iup
4. tar -zxvf iup-3.32_Linux515_64_lib.tar.gz -C ./iup
